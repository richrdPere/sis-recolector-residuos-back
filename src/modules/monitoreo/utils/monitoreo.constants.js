const PROGRAMMING_STATES = [
    'PROGRAMADA',
    'ASIGNADA',
    'ACEPTADA',
    'EN_CURSO',
    'PAUSADA',
    'FINALIZADA',
    'CANCELADA',
];

const ACTIVE_PROGRAMMING_STATES = [
    'EN_CURSO',
    'PAUSADA',
];

const ROUTE_STATES = [
    'EN_CURSO',
    'PAUSADO',
    'FINALIZADO',
    'CANCELADO',
];

const ACTIVE_ROUTE_STATES = [
    'EN_CURSO',
    'PAUSADO',
];

const ALERT_LEVELS = [
    'INFORMATIVA',
    'ADVERTENCIA',
    'CRITICA',
];

const DEFAULT_GPS_ONLINE_SECONDS =
    Number(process.env.MONITOR_GPS_ONLINE_SECONDS) || 60;

const DEFAULT_GPS_DELAYED_SECONDS =
    Number(process.env.MONITOR_GPS_DELAYED_SECONDS) || 180;

const DEFAULT_START_TOLERANCE_MINUTES =
    Number(process.env.MONITOR_START_TOLERANCE_MINUTES) || 15;

const DEFAULT_PAUSE_ALERT_MINUTES =
    Number(process.env.MONITOR_PAUSE_ALERT_MINUTES) || 30;

const DEFAULT_CAPACITY_WARNING_PERCENT =
    Number(process.env.CAPACITY_WARNING_PERCENT) || 80;

const DEFAULT_CAPACITY_CRITICAL_PERCENT =
    Number(process.env.CAPACITY_CRITICAL_PERCENT) || 100;

module.exports = {
    PROGRAMMING_STATES,
    ACTIVE_PROGRAMMING_STATES,
    ROUTE_STATES,
    ACTIVE_ROUTE_STATES,
    ALERT_LEVELS,
    DEFAULT_GPS_ONLINE_SECONDS,
    DEFAULT_GPS_DELAYED_SECONDS,
    DEFAULT_START_TOLERANCE_MINUTES,
    DEFAULT_PAUSE_ALERT_MINUTES,
    DEFAULT_CAPACITY_WARNING_PERCENT,
    DEFAULT_CAPACITY_CRITICAL_PERCENT,
};